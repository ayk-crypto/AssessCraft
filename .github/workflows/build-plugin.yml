name: Build installable plugin

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install production dependencies
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
          tools: composer:v2

      - run: composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader

      - name: Assemble clean plugin directory
        run: |
          mkdir -p build/assesscraft
          rsync -a \
            --exclude '.git' \
            --exclude '.gitignore' \
            --exclude '.editorconfig' \
            --exclude '.github/' \
            --exclude '.wordpress-org/' \
            --exclude 'build/' \
            --exclude 'docs/' \
            --exclude 'tests/' \
            --exclude 'composer.*' \
            --exclude 'README.md' \
            --exclude 'RELEASE_NOTES.md' \
            --exclude 'CHECKSUMS.txt' \
            --exclude 'php*.xml*' \
            --exclude '*.zip' \
            ./ build/assesscraft/

      - name: Create ZIP
        run: cd build && zip -qr assesscraft-free-0.18.0.zip assesscraft

      - name: Verify ZIP
        run: unzip -t build/assesscraft-free-0.18.0.zip

      - name: Create checksum
        run: cd build && sha256sum assesscraft-free-0.18.0.zip > assesscraft-free-0.18.0.zip.sha256

      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: assesscraft-plugin
          path: |
            build/assesscraft-free-0.18.0.zip
            build/assesscraft-free-0.18.0.zip.sha256
          if-no-files-found: error
